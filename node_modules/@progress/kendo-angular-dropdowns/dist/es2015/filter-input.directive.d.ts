import { ElementRef, DoCheck, KeyValueDiffers } from '@angular/core';
/**
 * @hidden
 */
export declare class FilterInputDirective implements DoCheck {
    private element;
    private differs;
    focused: any;
    constructor(element: ElementRef, differs: KeyValueDiffers);
    ngDoCheck(): void;
    private _focused;
    private differ;
}
