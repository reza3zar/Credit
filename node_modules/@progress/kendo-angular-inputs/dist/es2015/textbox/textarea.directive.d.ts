import { ElementRef, EventEmitter, OnDestroy, Renderer2, OnChanges, OnInit, NgZone } from '@angular/core';
import { NgControl } from '@angular/forms';
/**
 * Represents the [Kendo UI TextArea directive for the Inputs components for Angular]({% slug overview_textarea %}).
 * Provides floating labels to `textarea` elements.
 *
 * @example
 * ```ts-no-run
 * <textarea kendoTextArea></textarea>
 * ```
 */
export declare class TextAreaDirective implements OnInit, OnDestroy, OnChanges {
    private renderer;
    private element;
    private zone;
    private control;
    readonly elementClass: boolean;
    /**
     * @hidden
     */
    onFocus: EventEmitter<any>;
    /**
     * @hidden
     */
    onBlur: EventEmitter<any>;
    /**
     * @hidden
     */
    onValueChange: EventEmitter<any>;
    /**
     * @hidden
     */
    direction: string;
    /**
     * Specifies if the `textarea` element will resize its height automatically
     * ([see example]({% slug overview_textarea %}#toc-auto-resizing)).
     *
     * @default false
     */
    autoSize: boolean;
    /**
     * @hidden
     */
    /**
     * @hidden
     */
    value: string;
    id: string;
    constructor(renderer: Renderer2, element: ElementRef, zone: NgZone, control: NgControl, rtl: boolean);
    ngOnChanges(changes: any): void;
    ngOnInit(): void;
    ngOnDestroy(): void;
    private listeners;
    private valueChangeSubscription;
    private inputSubscription;
    private initialHeight;
    private resize();
}
